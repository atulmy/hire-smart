// UI Imports
import grey from '@material-ui/core/colors/grey'

// Component Styles
const styles = theme => ({
  root: {
    paddingTop: '10vh',
  },

  container: {
    width: '30vw',
    margin: '0 auto',
    padding: theme.spacing.unit * 2,
  },

})

export default styles
