// Component Styles
const styles = theme => ({
  textCenter: {
    textAlign: 'center'
  },

  buttonsContainer: {
    textAlign: 'right',
    marginTop: theme.spacing()
  },

  formContainer: {
    padding: theme.spacing(2)
  },

  list: {
    maxHeight: 'calc(100vh - 248px)',
    overflowY: 'auto'
  },
})

export default styles
