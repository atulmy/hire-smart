// Component Styles
const styles = theme => ({
  root: {
    padding: theme.spacing.unit * 2,
    minWidth: 350
  },

  title: {
    marginBottom: theme.spacing.unit * 3,
  }
})

export default styles
