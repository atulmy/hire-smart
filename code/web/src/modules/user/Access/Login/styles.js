// Component Styles
const styles = theme => ({
  root: {
    padding: theme.spacing()
  },

  buttonsContainer: {
    textAlign: 'right',
    marginTop: theme.spacing()
  },

  heading: {
    marginBottom: theme.spacing(2)
  }
})

export default styles
