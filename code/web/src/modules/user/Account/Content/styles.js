// UI Imports
import grey from '@material-ui/core/colors/grey'

// Component Styles
const styles = theme => ({
  root: {
    minHeight: 'calc(100vh - 100px)',
    flex: 1
  }
})

export default styles
